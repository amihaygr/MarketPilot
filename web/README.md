# Web App

The Web App communicates only with the Backend API.

It must not contain:

- MariaDB credentials;
- MinIO credentials;
- Alpaca secrets;
- direct object-storage access.

The UI should display data freshness and provisional or certified publication status.
