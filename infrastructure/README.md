# Infrastructure

Planned infrastructure definitions:

- Docker Compose
- Kafka KRaft configuration
- Spark Master and Worker configuration
- MariaDB initialization
- MinIO initialization
- Airflow infrastructure in a later phase

Phase 1 must create the core Compose implementation only after Codex proposes and the user approves a compatibility-aware plan.
