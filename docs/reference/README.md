# Architecture Sources

The Markdown documents and Mermaid sources under `docs/architecture/` are the only current architecture source of truth.

Legacy Word and PDF diagrams are intentionally excluded from this rebuilt repository because embedded images can become stale. Render Mermaid locally in VS Code or in the repository preview so the diagram always matches its source.
